---
title: RCON / расширенные команды
sidebar_position: 40
---

# RCON / расширенные команды

**Из чата:**  
- `!<id> rcon !map de_dust2` — смена карты  
- `!<id> cmd <команда>` — выполнить серверную команду  

Требуются повышенные права (настраиваются в чате).

**Примечания:**  
- Типы команд `rcon` и `cmd` равнозначны.  
- В `!map` символ `!` автоматически заменяется на `sm_` для корректной работы (удобство ввода).  
- В `! <id>` пробел между `!` и `<id>` удаляется автоматически — можно писать с пробелом или без.  
- **RCON-доступ к серверу не используется!** Команды исполняются через внутреннюю систему.  
- Команда `map` (мягкий рестарт сервера) заблокирована и автоматически изменяется на `sm_map` (смена карты) для защиты от несанкционированного рестарта игрового сервера.
---

## Просмотр статуса сервера

**Команда:**  
```
!<id>
```
*(без аргументов)* — выводит сводную информацию о сервере.

**Пример вывода:**
```

Сервер: | Pro Server |  \[CS\:GO]
IP: 300.300.300.300:9000
20 игроков из 64 (ботов: 4), карта: de\_dust2

[Команда] <У/С> (Время) - Ник:
[🔥] <6/2> (0ч 13m 21s) | БаТя ДеДуШкИ
[🧊] <13/12> (0ч 31m 20s) | БОЛОТНЫЙ ГИГАНТ
[🐢] <9/5> (0ч 33m 21s) | Хирург
[👀] <0/0> (8ч 6m 37s) | drozd
[🤖] <-7/10> (1ч 8m 5s) | Кунг Падла
[🤖] <-7/8> (2ч 37m 55s) | maybe Про

```